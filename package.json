{
  "name": "helping-js",
  "version": "0.0.1",
  "author": "parsa jiravand",
  "description": "simple utils for javascript",
  "main": "./index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "uglifyjs ./core.js --compress --mangle --comments --output ./index.js",
    "build:es5": "babel src -d core"
  },
  "license": "MIT",
  "files": [
    "core"
  ],
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "uglify-js": "^3.17.4"
  }
}
